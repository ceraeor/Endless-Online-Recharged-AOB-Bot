This folder contains configuration files used by CeraBot.

The bot automatically generates and saves configuration settings here.

Example config file:
cerabot_config.json

You normally do not need to edit this manually. The GUI will manage settings automatically.

You can delete the config file if you want the bot to regenerate default settings.
